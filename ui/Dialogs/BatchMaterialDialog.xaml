<Window x:Class="BOMManager.UI.Dialogs.BatchMaterialDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="재질 일괄 지정 (Batch Material)"
        Width="380" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI, Malgun Gothic">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="lblInfo"
                   Text="선택한 파트에 적용할 재질을 선택하거나 입력하세요:"
                   TextWrapping="Wrap"
                   Foreground="#1E293B"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <ComboBox Grid.Row="1" x:Name="comboMaterial"
                  IsEditable="True"
                  Height="32"
                  FontSize="13"
                  Padding="6,4">
            <ComboBoxItem Content="AL 60"/>
            <ComboBoxItem Content="SUS"/>
            <ComboBoxItem Content="ULTEM 2300"/>
            <ComboBoxItem Content="SS400"/>
            <ComboBoxItem Content="SUS304"/>
            <ComboBoxItem Content="AL6061-T6"/>
            <ComboBoxItem Content="S45C"/>
            <ComboBoxItem Content="SCM440"/>
            <ComboBoxItem Content="SKD11"/>
            <ComboBoxItem Content="POM"/>
            <ComboBoxItem Content="MC NYLON"/>
            <ComboBoxItem Content="URETHANE"/>
            <ComboBoxItem Content="BRASS"/>
            <ComboBoxItem Content="SPRING STEEL"/>
        </ComboBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="확인" Width="80" Height="30" Margin="0,0,8,0"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="BtnOk_Click"/>
            <Button Content="취소" Width="80" Height="30"
                    IsCancel="True"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
